<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <!-- Use: dotnet publish -c Release -r linux-x64 -p:PublishSingleFile=true -p:SelfContained=true -->
    <!-- or:   dotnet publish -c Release -r win-x64 -p:PublishSingleFile=true -p:SelfContained=true -->
  </PropertyGroup>

</Project>
